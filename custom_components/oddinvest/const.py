"""Константи інтеграції ODD Invest."""

DOMAIN = "oddinvest"

CONF_BASE_URL = "base_url"
CONF_TOPIC_PREFIX = "topic_prefix"
# CONF_TOKEN — Bearer для REST, коли на сервісі стоїть замок. Береться в
# самому застосунку: «Налаштування → Доступ ззовні → Видати токен».
# Порожньо = сервіс без пароля (перший запуск).
#
# Публічної адреси тут НЕМАЄ навмисно: її задає сам сервіс при підключенні
# тунелю й публікує в документі стану (settings.public_url), звідки її бере
# OddInvestData.open_url. Поле інтеграції для тієї самої адреси було б
# другим джерелом однієї правди — і застаріло б рівно тоді, коли адресу
# міняли. REST при цьому й далі ходить на base_url у локальній мережі: за
# зовнішньою адресою може стояти другий замок (Cloudflare Access), якого
# машина не пройде.
CONF_TOKEN = "token"

DEFAULT_PREFIX = "oddinvest"

SIGNAL_STATE_UPDATED = f"{DOMAIN}_state_updated"
SIGNAL_AVAILABILITY = f"{DOMAIN}_availability"

SUPPORTED_SCHEMA = 1

SERVICE_REFRESH = "refresh"
SERVICE_MARK_PAYMENT = "mark_payment"

# STALE_AFTER_H — за скільки годин стан вважається несвіжим.
#
# Арифметика така. Добова джоба сервіса ходить у НБУ о 06:10 за Києвом,
# тобто між двома успішними оновленнями рівно 24 години, і найтугіша
# чесна межа — приблизно 30 (24 плюс запас на довгий бекфіл і на те, що
# документ публікується не миттєво). 36 лишає зверху ще один пропущений
# запуск: одна невдала спроба — не привід будити людину, дві поспіль —
# уже привід.
#
# Живе тут, а не в binary_sensor.py, бо поріг спільний для сенсора й для
# сповіщень: два числа розійшлись би, і сутність казала б одне, а пуш —
# інше про ту саму відмову.
STALE_AFTER_H = 36

# CARD_MARK_STALE_DAYS — з якого віку звірка картки перестає бути виміром.
#
# Те саме число, що в сервісі (cardMarkStaleDays у state_tasks.go): два
# тижні, бо пільговий цикл місячний, і звірка, старша за півцикла, не
# встигає попередити про той самий цикл, про який говорить. Живе тут, бо
# спільне для бінарного сенсора й для сповіщення.
CARD_MARK_STALE_DAYS = 14

# CARD_DUE_SOON_DAYS — за скільки днів до розрахункової дати картка
# вважається «на носі». Сім, як у сервісі: там задача card-due-* саме з
# цього дня переходить у «зараз».
CARD_DUE_SOON_DAYS = 7
